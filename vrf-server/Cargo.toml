[package]
name = "vrf-server"
version = "0.1.0"
edition = "2021"

[dependencies]
anchor-client = "0.26"

once_cell = "1.17"
regex = "1.7"

base64 = "0.21"

vrf = "0.2"
vrf-sdk = { path = "../vrf-sdk" }
